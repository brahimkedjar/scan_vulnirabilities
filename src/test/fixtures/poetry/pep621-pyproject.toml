[project]
name = "poetry-pep621-fixture"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
  "requests>=2.31,<3",
]

[build-system]
requires = ["poetry-core>=2.0"]
build-backend = 'poetry.core.masonry.api'
